所有的用户信息放在
/etc/passwd
所有加密后的密码放在
/etc/shadow
所有组放在
/etc/group

权限问题
terry  terry
所有者 所属组

ls -lh 添加上h 可以查看文件的大小跟修改的时间

drwxrwxr-x
1、代表文件的类型
     -代表一个二进制文件
     d代表一个目录
     b代表一个设备文件
     c代表设备文件（字符型文件）
     l快捷方式

2、
     U       G       O
   所有者  所属组  其他人
r 读4
w 写2
x 执行1


更改子文件夹里面的权限
chmod -R 777 文件

改变文件所有组 (-R)递归
chown -R 

创建用户
useradd tom

chown tom:tom a/ 改变文件所有者所有组

file 文件  查看文件的类型

目录文件详细介绍
bin 存放的二进制文件
boot 存放的是系统的启动文件，存放的文件的内核文件
dev 存放的设备文件
etc 存放的是一些配置文件
home 存放的是（D-S）
lib 里面存放的是一些链接库（动态链接库）
lost-found 
media 自动挂载点，（优盘）
mnt 挂载点
opt 当我们取装一个软件包的时候（源码包安装）不指名安装目录 默认安装在这
proc 里面的东西不占用磁盘的大小
root 其他用户的目录放在home 
sbin 里面存放的是二进制蚊子，里面存放的是
tmp 里面存放的是一些临时性的文件
usr 默认是装在usr下
var 默认存放的是一些缓存，日志

cd .. 上一级目录
cd ../..  上一级的上一级目录

 
































      
